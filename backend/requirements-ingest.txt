# Extra dependencies required ONLY to run the online ingestion pipeline
# (POST /api/ingest) inside the backend process. Install on top of
# requirements.txt when the backend host will execute ingestion jobs:
#
#     pip install -r requirements.txt -r requirements-ingest.txt
#
# These mirror database/requirements.txt (the pipeline lives in database/src).
-r requirements.txt

pandas
numpy
opencv-python
PyMuPDF
webvtt-py
google-genai
langchain-huggingface
langchain-core
huggingface_hub
